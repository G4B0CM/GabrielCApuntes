<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:GabrielCApuntes.Models"
             x:Class="GabrielCApuntes.Views.AboutPage">
    <ContentPage.BindingContext>
        <models:About />
    </ContentPage.BindingContext>
    <VerticalStackLayout Spacing="10" Margin="10">
        <Label FontSize="30" FontAttributes="Bold" Text="Gabriel Calderón App Apuntes" VerticalOptions="End"/>
        <HorizontalStackLayout Spacing="10">
            <Image Source="https://i.ytimg.com/vi/PcYHjN21nIU/hqdefault.jpg"
                   SemanticProperties.Description="Broma cibernetica"
                   HeightRequest="64" />
            <Label FontSize="22" FontAttributes="Bold" Text="{Binding Title}" VerticalOptions="End" />
            <Label FontSize="22" Text="{Binding Version}" VerticalOptions="End" />
        </HorizontalStackLayout>

        <Label Text="{Binding Message}" />
        <Button Text="Learn more..." Clicked="LearnMore_Clicked" />
    </VerticalStackLayout>
</ContentPage>